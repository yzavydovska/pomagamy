# 05 — Panel administratora — zakładki i dashboard

- **Trzy zakładki** (`AdminTabParamList`): **`AdminDashboard`** (liczniki/strzał podsumowań z Firestore przy chmurze), **`AdminWeryfikacjaOrg`** (lista organizacji oczekujących lub odrzuconych), **`AdminSkargi`** (zarządzanie zgłoszeniami/problemami).
- Przejście ze stacku **`AdminMainStack`**: zakładki + ekrany szczegółów pojedynczej skargi (`AdminComplaintDetail`) tryb **`firebase`** vs **`mock`**.
